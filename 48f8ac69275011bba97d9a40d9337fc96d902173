{"commit_hash":"48f8ac69275011bba97d9a40d9337fc96d902173","commit_message":"docs: update for Cursor support and model/agent commands","branch":"add-cursor-support","timestamp":"2026-03-02T16:09:27+01:00","files_changed":["CLAUDE.md","README.md","tests/cli_test.rs"],"decisions":[{"title":"Chose adding `shadw agent` and renaming `shadw use` to `shadw model` over keeping the old command names","summary":"The goal was to make the CLI more expressive and consistent, especially as new features like Cursor support were added.  The discussion weighed keeping the legacy `shadw use` command against introducing a clearer `shadw model` name that pairs naturally with a new `shadw agent` command.  The decision to rename was driven by the desire for a symmetrical API: `shadw model` selects the extraction model while `shadw agent` selects the AI agent, simplifying the user experience and reducing confusion.\n\nThe new `shadw agent` command also brings practical benefits: it updates the project’s `config.toml` and triggers an automatic daemon restart when the agent changes, ensuring the runtime environment stays in sync.  The status command was extended to detect the presence of a Cursor workspace or a Claude‑code project, allowing the tool to report the correct agent and model context.\n\nImplementation involved adding a new subcommand to the CLI, updating the README and CLAUDE documentation, and adjusting tests to cover the new behavior.  The change keeps backward compatibility by removing the old `shadw use` name, which was no longer needed once the new command pair was in place."}],"extracted_at":"2026-03-02T15:09:32.617634+00:00","model":"gpt-oss"}
