{"commit_hash":"2e55eda938d97b0e208b06ca0de60ce8b310a656","commit_message":"Merge pull request #19 from kzkr/new-models","branch":"add-cursor-support","timestamp":"2026-03-01T19:23:01+01:00","files_changed":[],"decisions":[{"title":"Chose Cursor watcher over polling fallback for conversation extraction","summary":"The team needed a second AI agent to broaden Shadw’s reach beyond Claude Code. The existing daemon hard‑coded a file‑watcher that only read JSONL logs, so the first decision was to abstract the watcher behind an `AgentWatcher` trait. This allowed a new `CursorWatcher` to be plugged in without touching the rest of the daemon.\n\nDuring the investigation the Cursor data model turned out to be split across a workspace‑specific SQLite database and a global `state.vscdb`. The conversation messages live under `cursorDiskKV` keys like `bubbleId:<composer>:<bubble>`. The plan therefore had to read that global store, filter by the current workspace ID, and translate the JSON blobs into the internal `Message` format.\n\nThe implementation added a `cursor.rs` module, registered it in the agent registry, and updated the CLI to expose a `shadw agent cursor` command. The code was then cleaned up: unused polling logic was removed, file‑system watchers were limited to the workspace directory, and platform‑specific paths were abstracted behind a helper. Tests were updated to cover the new watcher, and the build produced no warnings on both macOS and Linux."}],"extracted_at":"2026-03-02T15:07:34.271808+00:00","model":"gpt-oss"}
